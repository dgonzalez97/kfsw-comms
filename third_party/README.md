# Third-party dependencies

The west workspace checks out libcsp at `third_party/libcsp` for use by
`kfsw-comms`. libcsp remains an independent upstream Git project pinned by the
K-FSW west manifest; its source is not vendored into this repository.
